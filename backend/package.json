{
  "name": "backend",
  "module": "index.ts",
 "scripts": {
    "build": "bun build index.ts --outfile dist/index.js",
    "start:dev": "bun --env-file=.local.env --watch run index.ts",
    "start:prod": "bun install && bun --env-file=.prod.env run dist/index.js"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "private": true,
  "type": "module",
  "dependencies": {
    "@clerk/express": "1.7.60",
    "@types/express": "^5.0.6",
    "cors": "2.8.5",
    "express": "5.2.1",
    "mongoose": "9.0.2",
    "socket.io": "4.8.2"
  }
}
